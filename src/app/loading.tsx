export default function Loading() {
    // You can add any UI inside Loading, including a Skeleton.
    return <h1 className="absolute top-1/2 right-[40%] text-primary-color text-xl capitalize font-semibold loading">loading</h1>
  }